# Pip pins from the Dockerfile, for local work outside the container.
# The canonical environment is the Dockerfile base image
# anyscale/ray-llm:2.55.1-py311-cu128; this file is the pip subset of it.
# Ray itself ships with that base image, so the ray pin below mirrors the
# image version rather than a Dockerfile pip pin.
# Note: vllm[runai] is unused by the code in this repo.

ray[serve]==2.55.1

torch==2.10.0
torchvision==0.25.0
matplotlib==3.10.1
diffusers==0.32.2
transformers==4.57.4
accelerate==1.5.2
xgboost==2.1.4
pytorch-lightning==2.5.1
pyarrow==19.0.1
datasets==3.5.0
evaluate==0.4.3
scikit-learn==1.6.1
torch-tb-profiler==0.4.3
tensorboard==2.19.0
sentence-transformers==5.2.2
textdistance==4.6.3
vllm[runai]==0.18.0
pandas==3.0.3
